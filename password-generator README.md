# 🌦 Weather Checker (OpenWeatherMap API)

A Python script that fetches the current weather for any city using the OpenWeatherMap API.

## 💡 Features
- Input a city name
- Get current temperature, weather condition, and humidity
- Uses free weather API

## 🚀 How to Run
1. Sign up at [OpenWeatherMap](https://openweathermap.org/api) to get your API key.
2. Replace `"YOUR_API_KEY_HERE"` in the code with your actual key.
3. Run:

```bash
python weather.py
